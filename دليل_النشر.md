# 🚀 دليل النشر المبسط - ANA DATA

## ❓ أولاً: كل سطر بيعمل إيه؟

```bash
cd backend              # ادخل مجلد الباك إند
pip install -r requirements.txt   # ثبّت المكتبات المطلوبة (Python)
cp .env.example .env    # انسخ ملف الإعدادات
python auth_telegram.py # سجّل دخول تليجرام (مرة واحدة بس)
python main.py          # شغّل السيرفر
```

**ببساطة**: السيرفر ده هو اللي بيكلم تليجرام ويجيب الأرقام الحقيقية.

---

## 🎯 عندك 3 اختيارات (اختار اللي يريحك)

---

### ✅ الاختيار 1: تشغيل على جهازك (أسهل حاجة، 5 دقايق)

**لو عندك ويندوز:**

1. نزّل Python من: https://www.python.org/downloads/ (اختار 3.10 أو أحدث)
2. افتح **Command Prompt** (cmd)
3. اكتب الأوامر دي واحدة واحدة:

```cmd
cd C:\Users\اسمك\Desktop\ana-data\backend
pip install -r requirements.txt
copy .env.example .env
python auth_telegram.py
```

4. لما يطلب منك الكود، افتح تلجرام على موبايلك → هتلاقي كود 5 أرقام → اكتبه
5. بعد كده:

```cmd
python main.py
```

6. اترك النافذة مفتوحة — السيرفر شغال
7. ارجع للموقع وحدّث — هتلاقي البحث بقى حقيقي!

> ⚠️ السيرفر هيشتغل بس لما الكمبيوتر يكون مفتوح والإنترنت شغال.

---

### ✅ الاختيار 2: نشر أونلاين على Railway (مجاني - أفضل طريقة)

**Railway** هو موقع بيشغل تطبيقات Python مجاناً.

1. ادخل على https://railway.app وسجّل حساب (بالـ GitHub)
2. ارفع المشروع على GitHub الأول:
   - اعمل repo جديد على GitHub
   - ارفع كل ملفات المشروع عليه
3. في Railway، اضغط **"New Project"** → **"Deploy from GitHub repo"**
4. اختار المشروع بتاعك
5. اضغط على المشروع → **Variables** → حط القيم دي:

```
API_ID = 36041141
API_HASH = 192dd9c6f9a766c7880bc74a59d53d2d
ADMIN_PHONE = +201143645282
ADMIN_EMAIL = mdyb7018@gmail.com
ADMIN_PASSWORD = admin123456
JWT_SECRET = أي-كلمة-سر-طويلة-وعشوائية
PORT = 8000
```

6. في Railway، اضغط **Settings** → **Deploy** → استنى يخلص
7. بعد ما يشتغل، في Railway هتلاقي رابط زي: `https://xxx.up.railway.app`
8. روح على السيرفر ده على المتصفح: `https://xxx.up.railway.app/auth_telegram`
   - أو استخدم Railway Terminal واكتب: `python auth_telegram.py`
   - اكتب كود تليجرام اللي وصلك
9. بعد كده ارجع للموقع → اضغط "إعداد رابط API" → حط رابط Railway
10. خلاص! الموقع بقى شغال بقاعدة بيانات تليجرام الحقيقية 24/7 🎉

---

### ✅ الاختيار 3: Render.com (مجاني برضو)

نفس الخطوات تقريباً، بس على https://render.com:
- New → Web Service → Connect GitHub
- Build Command: `pip install -r requirements.txt`
- Start Command: `python main.py`
- Plan: Free
- حط نفس Environment Variables زي ما في الاختيار 2

---

## 🤔 إيه اللي هيحصل لما تشغّل الباك إند؟

1. **أول مرة بس**: تليجرام هيبعتلك كود على الموبايل
2. **بعدها**: السيرفر بيفضل شغال ويجيب أرقام من تليجرام تلقائياً
3. **النتيجة**: لما حد يدخل على موقعك ويدور على يوزر، الموقع هيكلم السيرفر، السيرفر هيكلم تليجرام، وهيعرض الرقم الحقيقي

---

## ❓ محتاج مساعدة؟

قولي على أي خطوة وقفت معاك وأنا هساعدك فيها فوراً 👇
